div.article-list#article-list-{{ module.getId() }} div.article h3.article-titre {
	background: #{{ configuration.getTitleBackgroundColor() }};
	color: #{{ configuration.getTitlesTextColor() }};
	padding: 5px;
}

div.article-list div.article div.article-contenu {
	padding: 5px;
}